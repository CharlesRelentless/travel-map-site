MOJI_APPCODE=replace-with-server-side-appcode
# Leave empty when the map and API are served by the same Render service.
TRAVEL_MAP_ALLOWED_ORIGIN=
# Optional local-only workaround. Do not enable in production.
MOJI_ALLOW_INSECURE_SSL=0
